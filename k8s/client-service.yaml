apiVersion: v1
kind: Service
metadata:
  name: todo-client-service
  namespace: todo
spec:
  selector:
    app: client
  type: NodePort
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30000    